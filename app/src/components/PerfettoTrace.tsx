// Compatibility entry while the remaining screen features move behind public
// feature APIs. New imports should use `features/trace` directly.
export { default } from '../features/trace/PerfettoTrace';
